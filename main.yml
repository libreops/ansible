---
- hosts: jupiter
  sudo: yes
  sudo_user: root

  roles:
    - common
    - { role: firewalld, tags: ['firewall'] }
    - { role: mariadb, tags: ['mariadb', 'database'] }
    - { role: nginx, tags: ['nginx'] }

- include: playbooks/pad.yml
